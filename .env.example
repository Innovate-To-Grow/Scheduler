# Frontend (loaded automatically by Create React App)
REACT_APP_API_URL=http://localhost:5000/api

# Backend (requires dotenv or must be set in shell environment)
PORT=5000
CORS_ORIGIN=http://localhost:3000
DB_PATH=./server/data/scheduler.db
